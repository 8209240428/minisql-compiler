# minisql-compiler
3 人协作的 MiniSQL 数据库（Java），从零实现 **词法 → 语法 → 语义 → 逻辑执行计划 → 优化 → 执行 → 存储 → 磁盘** 的端到端流水线， 既有 Swing 交互界面，也有命令行终端（CLI）。产品不仅产出并可视化**逻辑执行计划**，还实现了**真实的行存储与执行**： 页式存储管理、LRU 缓冲池、记录/目录序列化、系统目录持久化、Volcano 执行引擎，数据落盘到 `.db` 文件，重开即恢复。
